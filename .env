# The URL should be your public backend address.
# The variable name MUST be VITE_API_BASE_URL.
VITE_API_BASE_URL=https://sipb.crulxproject.com/api/v1

# This variable is for cookie-based authentication. While you are using tokens,
# it's good practice to keep it configured correctly.
VITE_COOKIE_DOMAIN=.crulxproject.com